## 複数行の文字列を扱う

```
PHPで複数行の文字列を扱うには、<<<EOT で始まるヒアドキュメント構文を使用します。ダブルクォーテーションを使わずに長文やHTML、SQL文をそのまま記述でき、変数展開や改行コードも自動で反映されます。
```

## phpをパソコン内で試す方法

phpを公式サイトからダウンロードします。 zipでダウンロード
ダウンロードしたzipを解凍
C:/直下にダウンロードしたフォルダを移動する。
移動したフォルダをshift+右クリックする。
その後、パスのコピーを行う。

下のウィンドウマークの横の検索窓をクリック。
環境変数と入力
検索結果をクリック

上下に環境変数のやつが出るので、上の方のpathをクリックして新規追加でコピーしたpathを張り付ける。
下のウィンドウマークの横の検索窓をクリック。
コマンドプロンプトと入力

phpと入力し、何も表示されないことを確認。
元のuserが表示されるとなにもされていないので失敗。


追加の処理

🛠 修正手順（3ステップ）
1. php.ini ファイルを開く


PHPがインストールされているフォルダ（例: C:\php など）の中にある php.ini というファイルをメモ帳などのテキストエディタで開きます。
:bulb: もし php.ini が見つからない場合 同じフォルダに php.ini-development というファイルがあるはずなので、それをコピーして名前を php.ini に変更してください。


2. extension_dir のコメントアウトを外す
ファイル内を extension_dir = "ext" で検索してください。 行の先頭に ;（セミコロン）がついている場合は、; を消して有効化します。

【修正前】
```
;extension_dir = "ext"
```
【修正後】
```
extension_dir = "ext"
```
3. mbstring を有効化する
次に、extension=mbstring で検索してください。 こちらも先頭の ; を消して有効化します。
【修正前】
```ini
;extension=mbstring
```
【修正後】
```ini
extension=mbstring
```
ファイルを保存して閉じます。


設定が終わったら、コマンドプロンプトに戻り、もう一度コマンドを実行してみてください。

```
php send.php
```
これでもう一度エラーが出る場合は、ターミナルを開き直す（一度閉じて新しく起動する）か、PCを再起動すると設定が確実に反映されます。試してみてください！したいhtml phpがあるフォルダまで行く。

phpにはお試し用のサーバーが入っているそうです。
そちらを用いてテストします。

-Sでそれを起動できるようです。
起動したいhtml内で右クリックでコマンドプロンプトを起動。

```
php -S localhost:8000
```
で起動する。

ブラウザで　http://localhost:8000/
で表示されたらＯＫ。